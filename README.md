# LiteBansNotify

LiteBansNotify интегрируется с плагином LiteBans и автоматически отправляет уведомления в Telegram чат о всех действиях модерации на сервере: баны, муты, кики, варны и их отмену.

## Возможности

- Уведомления о наказаниях:
  - Бан (временный и постоянный)
  - Мут (временный и постоянный)
  - Кик
  - Варн
- Уведомления о снятии наказаний:
  - Разбан
  - Размут
  - Снятие варна
- Полностью настраиваемые сообщения через конфигурацию
- Поддержка HTML форматирования в Telegram

## Требования

- Minecraft 1.16+
- Java 8+
- [LiteBans](https://www.spigotmc.org/resources/3715/) плагин
- Telegram бот (получить токен у [@BotFather](https://t.me/botfather))

## Установка

1. Скачайте последнюю версию плагина из [Actions](../../actions) (выберите последний успешный workflow и скачайте артефакт)
2. Поместите `.jar` файл в папку `plugins` вашего сервера
3. Убедитесь, что плагин LiteBans установлен и работает
4. Перезапустите сервер
5. Настройте конфигурацию (см. раздел "Настройка")

## Настройка

После первого запуска будет создан файл `config.yml` в папке `plugins/LiteBansNotify/`.

### Основные параметры

```yaml
patternTime: dd.MM.yy HH:mm

telegram:
  enable: true              # Включить/выключить отправку уведомлений
  token: TOKEN_BOT_API      # Токен вашего Telegram бота
  id: -1002100000000        # ID чата для отправки уведомлений
  theme: 52                 # ID темы (топика) в чате (опционально)
```

### Получение необходимых данных

#### Токен бота:
1. Напишите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен

#### ID чата:
1. Добавьте бота в нужный чат/группу
2. Отправьте любое сообщение в чат
3. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
4. Найдите значение `"chat":{"id":`

#### ID темы (опционально):
- Если используете темы в группе, откройте нужную тему и найдите её ID в URL

### Настройка сообщений

Все сообщения полностью настраиваются через конфигурацию. Доступные плейсхолдеры:

- `{player}` - Имя нарушителя
- `{exec_player}` - Имя модератора
- `{time}` - Дата наказания
- `{time_end}` - Длительность/дата окончания наказания
- `{reason}` - Причина наказания
- `{servers}` - Режим применения (все серверы/конкретный сервер)

Пример настройки сообщения о бане:

```yaml
messages:
  ban:
    - 🚫 <b>БАН</b>
    - <b> </b>
    - <b>Нарушитель:</b> {player}
    - <b>Наказал:</b> {exec_player}
    - <b>Дата:</b> {time}
    - <b>Длительность:</b> {time_end}
    - <b>Причина:</b> {reason}
    - <b>Режим:</b> {servers}
```

## Команды

| Команда | Описание | Права |
|---------|----------|-------|
| `/litebansnotify` | Показать список команд | `litebansnotify.admin` |
| `/litebansnotify reload` | Перезагрузить конфигурацию | `litebansnotify.admin` |
| `/litebansnotify test` | Отправить тестовое сообщение | `litebansnotify.admin` |

## Права

- `litebansnotify.admin` - Доступ ко всем командам плагина (по умолчанию только для операторов)

## Сборка из исходников

```bash
git clone https://github.com/GG-MD/LiteBansNotify.git
cd LiteBansNotify
mvn clean package
```

Готовый `.jar` файл будет находиться в папке `target/`.

## Поддержка

Если у вас возникли проблемы или есть предложения:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что конфигурация заполнена корректно
3. Проверьте логи сервера на наличие ошибок
4. Создайте [Issue](../../issues) с подробным описанием проблемы

## Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

---

Сделано с любовью для Minecraft сообщества
